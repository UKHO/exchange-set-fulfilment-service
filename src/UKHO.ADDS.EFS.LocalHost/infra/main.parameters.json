{
    "$schema": "https://schema.management.azure.com/schemas/2019-04-01/deploymentParameters.json#",
    "contentVersion": "1.0.0.0",
    "parameters": {
      "principalId": {
        "value": "${AZURE_PRINCIPAL_ID}"
      },
      "addsEnvironment": {
        "value": "${AZURE_ADDS_ENVIRONMENT}"
      },
      "efs_redis_password": {
        "value": "${AZURE_EFS_REDIS_PASSWORD}"
      },
      "efsLogAnalyticsWorkspaceName": {
        "value": "${AZURE_EFS_LOG_ANALYTICS_WORKSPACE_NAME}"
      },
      "efsRetainResourceGroup": {
        "value": "${AZURE_EFS_RETAIN_RESOURCE_GROUP}"
      },
      "efsServiceIdentityName": {
        "value": "${AZURE_EFS_SERVICE_IDENTITY_NAME}"
      },
      "subnetResourceId": {
        "value": "${AZURE_SUBNET_RESOURCE_ID}"
      },
      "zoneRedundant": {
        "value": "${AZURE_ZONE_REDUNDANT}"
      },
      "environmentName": {
        "value": "${AZURE_ENV_NAME}"
      },
      "location": {
        "value": "${AZURE_LOCATION}"
      }
    }
  }
  