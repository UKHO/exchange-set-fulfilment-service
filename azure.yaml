# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: exchange-set-fulfilment-service
services:  
  app:
    language: dotnet
    project: ./src/UKHO.ADDS.EFS.LocalHost/UKHO.ADDS.EFS.LocalHost.csproj
    host: containerapp
